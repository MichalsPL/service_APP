{% extends "ServiceBundle:User:base_user.html.twig" %}
{% block page_content %}

    <div class="panel-body">
        {{message}}
        {%if home is defined%}
            <a  href="{{ path('user_index') }}">Wróć na stronę Główną</a>
        {%endif%}

        <div class="form-group">
            {%if form is defined%}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            {%endif%}

        </div>

    {% endblock %}
